class Persona {
  constructor(nombre, apellido, puesto) {
    (this.nombre = nombre), (this.apellido = apellido), (this.puesto = puesto);
  }
}

class Producto {
  constructor(marca, rubro, nombre, precio) {
    (this.marca = marca),
      (this.rubro = rubro),
      (this.nombre = nombre),
      (this.precio = precio);
  }
}
